import MainWrapper from '../components/MainWrapper';

export default function Home({ data }) {
  return (
    <MainWrapper>
      <main>
        Some content here
      </main>
    </MainWrapper>
  )
}